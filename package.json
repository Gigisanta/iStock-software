{
  "name": "istock",
  "private": true,
  "version": "0.0.0",
  "packageManager": "pnpm@10.34.5",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "typecheck": "pnpm -r --no-bail typecheck",
    "build": "pnpm -r --no-bail build",
    "lint": "pnpm -r --no-bail lint",
    "test": "pnpm -r --no-bail test",
    "dev": "pnpm --filter @istock/web dev",
    "e2e": "pnpm --filter @istock/e2e e2e",
    "db:generate": "pnpm --filter @istock/db generate",
    "db:migrate": "pnpm --filter @istock/db migrate",
    "db:seed": "MEDIA_LOCAL_ROOT=$(pwd)/.media-local pnpm --filter @istock/db seed && MEDIA_LOCAL_ROOT=$(pwd)/.media-local pnpm --filter @istock/web seed:demo-media",
    "db:local": "./scripts/pg-local.sh",
    "vercel:preflight": "./scripts/preflight-vercel-production.sh",
    "guard": "./scripts/guard-artifacts.sh --harness",
    "audit": "pnpm audit --audit-level=high"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vitest": "^3.2.4",
    "@types/node": "^22.18.8",
    "eslint": "^9.38.0",
    "js-yaml": "^4.1.0",
    "tsx": "^4.20.6"
  }
}
