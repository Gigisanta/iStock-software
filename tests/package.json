{
  "name": "@istock/tests",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "node ./scripts/qa-lint.mjs ."
  },
  "devDependencies": {
    "@istock/db": "workspace:*",
    "@istock/domain": "workspace:*",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
